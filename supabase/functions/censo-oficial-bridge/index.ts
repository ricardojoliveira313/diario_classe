const DEPLOYMENT_ID = 'AKfycbxRAI9YowrLP1ffKAV7URQaXWPKaOOV3dqDbxVouD7Q4Jq-lRFJDmVbzbeRahNpDv6STg';
const OFFICIAL_BASE = `https://script.google.com/macros/s/${DEPLOYMENT_ID}`;
const SCHOOL = 'EMEIEF LUIZ GONZAGA';
const ALLOWED_ORIGINS = new Set(['https://diario.jroapp.com.br','http://localhost:5173','http://127.0.0.1:5173']);
const MODALIDADES = new Set(['Educação Infantil','Ensino Fundamental Regular','Educação Básica I','Educação Básica II','Educação Física','Arte – Regular','EJA I','EJA II – Arte','EJA II – História','EJA II – Geografia','EJA II – Língua Portuguesa','EJA II – Matemática','EJA II – Ciências','EJA II – Inglês']);
const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!;
const SERVICE_KEY = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
const norm = (v: unknown) => String(v ?? '').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9 ]/g,' ').replace(/\s+/g,' ').trim();
const dig = (v: unknown) => String(v ?? '').replace(/\D/g,'');
const tokenHex = () => { const b=new Uint8Array(32); crypto.getRandomValues(b); return [...b].map(x=>x.toString(16).padStart(2,'0')).join(''); };
async function sha256(v:string){const h=new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v)));return [...h].map(x=>x.toString(16).padStart(2,'0')).join('');}
function headers(req:Request){const origin=req.headers.get('origin')||'';const h:Record<string,string>={'content-type':'application/json; charset=utf-8','access-control-allow-headers':'content-type,x-censo-token','access-control-allow-methods':'POST,OPTIONS','vary':'Origin','cache-control':'no-store','pragma':'no-cache','x-content-type-options':'nosniff'};if(ALLOWED_ORIGINS.has(origin))h['access-control-allow-origin']=origin;return h;}
function json(req:Request,data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:headers(req)});}
async function rest(path:string,init:RequestInit={}){const h=new Headers(init.headers||{});h.set('apikey',SERVICE_KEY);h.set('authorization',`Bearer ${SERVICE_KEY}`);if(init.body&&!h.has('content-type'))h.set('content-type','application/json');const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{...init,headers:h});const t=await r.text();if(!r.ok)throw new Error(`Banco ${r.status}`);if(!t)return null;try{return JSON.parse(t)}catch{return t}}
function parseAppsScript(text:string){const clean=text.replace(/^\)\]\}'\s*/,'').trim();let arr:any;try{arr=JSON.parse(clean)}catch{throw new Error('Resposta oficial em formato inesperado.')}if(!Array.isArray(arr))throw new Error('Resposta oficial inválida.');if(arr.find((x:any)=>Array.isArray(x)&&x[0]==='er'))throw new Error('O formulário oficial recusou a operação.');const op=arr.find((x:any)=>Array.isArray(x)&&x[0]==='op.exec');if(!op||!Array.isArray(op[1]))throw new Error('O formulário oficial não retornou resultado.');const p=op[1][1];if(typeof p!=='string')return p;try{return JSON.parse(p)}catch{return p}}
async function officialCall(fn:string,args:unknown[]=[]){const request=[fn,JSON.stringify(args),null,[0],null,null,true,0];const body=`request=${encodeURIComponent(JSON.stringify(request))}`;const ctrl=new AbortController();const timer=setTimeout(()=>ctrl.abort(),25000);try{const r=await fetch(`${OFFICIAL_BASE}/callback?nocache_id=${Date.now()}_${crypto.randomUUID()}`,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded;charset=UTF-8','x-same-domain':'1'},body,signal:ctrl.signal});const t=await r.text();if(!r.ok)throw new Error(`Formulário oficial HTTP ${r.status}.`);return parseAppsScript(t)}finally{clearTimeout(timer)}}
async function authenticate(username:string,password:string){if(norm(username)!=='RICOJOLIVEIRA'||!password||password.length>256)return null;const rows=await rest('rpc/verificar_login_censo',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({p_nome:username.trim(),p_senha:password})}) as any[];const row=Array.isArray(rows)?rows[0]:null;if(!row||row.bloqueado===true||row.perfil!=='admin'||norm(row.nome)!=='RICOJOLIVEIRA')return null;await rest(`CensoSessaoToken?usuario=eq.${encodeURIComponent(username.trim())}`,{method:'DELETE'});const raw=tokenHex();const hash=await sha256(raw);const expira=new Date(Date.now()+8*60*60*1000).toISOString();await rest('CensoSessaoToken',{method:'POST',headers:{'content-type':'application/json','prefer':'return=minimal'},body:JSON.stringify({token:hash,token_hash:hash,usuario:username.trim(),expira_em:expira})});return{token:raw,usuario:username.trim(),expiraEm:expira}}
async function validateToken(raw:string|null){if(!raw||!/^[0-9a-f]{64}$/i.test(raw))return null;const hash=await sha256(raw);const rows=await rest(`CensoSessaoToken?token_hash=eq.${encodeURIComponent(hash)}&select=usuario,expira_em&limit=1`) as any[];const row=Array.isArray(rows)?rows[0]:null;if(!row||norm(row.usuario)!=='RICOJOLIVEIRA'||new Date(row.expira_em).getTime()<=Date.now())return null;return row}
function ehDocente(cargo:unknown){const c=norm(cargo);return c.includes('PROFESSOR')||/^PROF\b/.test(c)}
async function attendance(rf:string){const rows=await rest(`CensoFrequenciaServidor?ano=eq.2026&mes=eq.9&rf=eq.${encodeURIComponent(dig(rf))}&select=rf,nome,cargo,lotacao,carga_horaria,pagina_pdf,local_trabalho&limit=2`) as any[];return Array.isArray(rows)?rows.find(x=>dig(x.rf)===dig(rf)&&ehDocente(x.cargo))||null:null}
let profilesCache:{at:number,rows:any[]}|null=null;
async function profiles(){if(profilesCache&&Date.now()-profilesCache.at<300000)return profilesCache.rows;const rows=await rest('profiles?select=id,nome,email,data_nascimento,cpf,endereco,bairro,cep,municipio,estado,telefone_celular_1,telefone_celular_2,telefone_fixo,cargo_funcao,horario_trabalho,registro_funcional_rf,etnia,nome_mae,nome_pai,municipio_nascimento,updated_at&limit=500') as any[];profilesCache={at:Date.now(),rows:Array.isArray(rows)?rows:[]};return profilesCache.rows}
function latest(rows:any[]){return[...rows].sort((a,b)=>new Date(b.updated_at||0).getTime()-new Date(a.updated_at||0).getTime())[0]||null}
async function localSources(rf:string,official:any){const all=await profiles();let profile=latest(all.filter(p=>dig(p.registro_funcional_rf)===dig(rf))),profileMatch=profile?'rf':'';if(!profile){profile=latest(all.filter(p=>official?.cpf&&dig(p.cpf)===dig(official.cpf)));if(profile)profileMatch='cpf'}if(!profile){profile=latest(all.filter(p=>norm(p.nome)===norm(official?.nome)));if(profile)profileMatch='nome'}let fichaRows=await rest(`CensoFichaServidor?ano=eq.2026&rf=eq.${encodeURIComponent(dig(rf))}&select=rf,nome,cpf,fonte_timestamp,metodo_match,dados&limit=1`) as any[];if((!fichaRows||!fichaRows.length)&&official?.cpf)fichaRows=await rest(`CensoFichaServidor?ano=eq.2026&cpf=eq.${encodeURIComponent(dig(official.cpf))}&select=rf,nome,cpf,fonte_timestamp,metodo_match,dados&limit=1`) as any[];const ficha=Array.isArray(fichaRows)?fichaRows[0]||null:null;let formacoes:any[]=[];if(profile?.id){const f=await rest(`formacoes?profile_id=eq.${encodeURIComponent(profile.id)}&select=tipo,curso,universidade,rede_ensino,modalidade,inicio,termino&order=created_at.asc`) as any[];formacoes=Array.isArray(f)?f:[]}return{profile,profileMatch,ficha,formacoes}}
function required(d:any){const p:string[]=[];if(d.unidadeEscolar!==SCHOOL)p.push('Unidade Escolar');if(!MODALIDADES.has(String(d.modalidade||'')))p.push('Modalidade');for(const[k,l]of [['rf','RF'],['nome','Nome'],['cpf','CPF'],['dataNasc','Nascimento'],['sexo','Sexo'],['corRaca','Cor/Raça'],['telefone','Telefone'],['email','E-mail'],['nacionalidade','Nacionalidade'],['cep','CEP'],['rua','Rua'],['bairro','Bairro'],['municipio','Município'],['uf','UF'],['grauFormacao','Grau de formação']]as const)if(!String(d[k]??'').trim())p.push(l);if(dig(d.cpf).length!==11)p.push('CPF válido');if(!Array.isArray(d.deficiencias)||!d.deficiencias.length)p.push('Deficiência');return[...new Set(p)]}
async function audit(rf:string,nome:string,usuario:string,status:string,resposta:any,modalidade?:string,requestId?:string){try{await rest('CensoEnvioLog',{method:'POST',headers:{'content-type':'application/json','prefer':'return=minimal'},body:JSON.stringify({rf:dig(rf),nome:String(nome||'').slice(0,200),usuario,status,resposta,modalidade:modalidade||null,request_id:/^[0-9a-f-]{36}$/i.test(String(requestId||''))?requestId:null})})}catch{}}
async function duplicate(rf:string,modalidade:string){const since=new Date(Date.now()-5*60*1000).toISOString();const rows=await rest(`CensoEnvioLog?rf=eq.${encodeURIComponent(dig(rf))}&modalidade=eq.${encodeURIComponent(modalidade)}&status=eq.enviado&criado_em=gte.${encodeURIComponent(since)}&select=id&limit=1`) as any[];return Array.isArray(rows)&&rows.length>0}
Deno.serve(async(req)=>{const origin=req.headers.get('origin');if(origin&&!ALLOWED_ORIGINS.has(origin))return json(req,{ok:false,erro:'Origem não autorizada.'},403);if(req.method==='OPTIONS')return new Response('',{status:204,headers:headers(req)});if(req.method!=='POST')return json(req,{ok:false,erro:'Método não permitido.'},405);try{const body=await req.json().catch(()=>({}));const action=String(body?.action||'');if(action==='auth'){const session=await authenticate(String(body.username||''),String(body.password||''));if(!session)return json(req,{ok:false,erro:'Integração direta não autorizada ou temporariamente bloqueada.'},401);return json(req,{ok:true,...session})}const session=await validateToken(req.headers.get('x-censo-token'));if(!session)return json(req,{ok:false,erro:'Sessão segura do Censo expirada. Desbloqueie novamente.'},401);if(action==='status')return json(req,{ok:true,modo:'direto-seguro',usuario:session.usuario});if(action==='logout'){const hash=await sha256(req.headers.get('x-censo-token')||'');await rest(`CensoSessaoToken?token_hash=eq.${encodeURIComponent(hash)}`,{method:'DELETE'});return json(req,{ok:true})}if(action==='lookup'||action==='prepare'){const rf=dig(body.rf);if(!rf)return json(req,{ok:false,erro:'RF obrigatório.'},400);const freq=await attendance(rf);if(!freq)return json(req,{ok:false,erro:'RF não consta como vínculo docente na frequência oficial de setembro/2026.'},403);const official=await officialCall('buscarServidorCenso',[rf]);if(!official?.sucesso)return json(req,{ok:false,erro:official?.mensagem||'RF não localizado no formulário oficial.'},404);if(action==='lookup')return json(req,{ok:true,official,attendance:freq});const local=await localSources(rf,official);return json(req,{ok:true,official,attendance:freq,local,unidadeEscolar:SCHOOL})}if(action==='submit'){if(body.confirmacaoFinal!==true)return json(req,{ok:false,erro:'A confirmação final é obrigatória.'},400);const d=body.dados||{};const problems=required(d);if(problems.length)return json(req,{ok:false,erro:`Revise os campos obrigatórios: ${problems.join(', ')}.`,campos:problems},400);const freq=await attendance(d.rf);if(!freq)return json(req,{ok:false,erro:'RF não consta como vínculo docente na frequência de setembro/2026. Nada foi enviado.'},403);if(body.reenviar!==true&&await duplicate(d.rf,d.modalidade))return json(req,{ok:false,erro:'Este vínculo/modalidade já foi enviado nos últimos 5 minutos. Aguarde antes de reenviar.',duplicado:true},409);const official=await officialCall('buscarServidorCenso',[dig(d.rf)]);if(!official?.sucesso)return json(req,{ok:false,erro:'RF deixou de ser reconhecido pelo formulário oficial.'},409);if(norm(official.nome)!==norm(d.nome)||dig(official.cpf)!==dig(d.cpf)){await audit(d.rf,d.nome,session.usuario,'bloqueado_identidade',{motivo:'identidade_divergente'},d.modalidade,body.requestId);return json(req,{ok:false,erro:'Nome/CPF não correspondem ao RF retornado pela Secretaria. Nada foi enviado.'},409)}const payload={rf:dig(d.rf),nome:d.nome,unidadeEscolar:SCHOOL,cpf:dig(d.cpf),dataNasc:d.dataNasc,sexo:d.sexo,corRaca:d.corRaca,telefone:d.telefone,email:d.email,filiacao1:d.nomeMae||'',filiacao2:d.nomePai||'',nacionalidade:d.nacionalidade,paisEstrangeiro:d.paisEstrangeiro||'',naturalidade:d.naturalidade||'',ufNascimento:d.ufNascimento||'',deficiencias:(Array.isArray(d.deficiencias)?d.deficiencias:[]).join(', '),cep:d.cep,rua:d.rua,numero:d.numero||'',bairro:d.bairro,municipio:d.municipio,uf:d.uf,modalidades:d.modalidade,grauFormacao:d.grauFormacao,cursosSuperiores:Array.isArray(d.cursosSuperiores)?d.cursosSuperiores.slice(0,3):[],naoPossuiPos:!Boolean(d.possuiPos),posGraduacoes:Array.isArray(d.posGraduacoes)?d.posGraduacoes.slice(0,6):[],cursosEspecificos:(Array.isArray(d.cursosEspecificos)?d.cursosEspecificos:[]).join(', ')};const result=await officialCall('processarFormularioCenso',[payload]);await audit(payload.rf,payload.nome,session.usuario,'enviado',{resultado:result},d.modalidade,body.requestId);return json(req,{ok:true,result,requestId:body.requestId||null})}return json(req,{ok:false,erro:'Ação desconhecida.'},400)}catch(e){return json(req,{ok:false,erro:e instanceof Error?e.message:String(e)},500)}});